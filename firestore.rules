rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write across all collections for Mobilis operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
